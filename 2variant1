import sys
a = list(map(str.strip, sys.stdin))
tbd = []
for i in a:
    temp = list(map(str, i.split(', ')))
    c = 0
    ere = []
    for _ in temp:
        if len(str(_)) == 1:
            c -= 1
            ere.append(_)
    tbd.append([ere, c])

tbd.sort(key=lambda x: x[1])

for i in tbd[0][0]:
    print(i, end=' ')
